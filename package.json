{
  "name": "@littlegun/detect-gpu",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "run-script-os",
    "build:default": "rm -rf dist && tsc && cp -r src/bin dist",
    "build:win32": "Remove-Item dist -Recurse -Force -ErrorAction SilentlyContinue && tsc && Copy-Item src/bin -Destination dist -Recurse -Force",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.1.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "run-script-os": "^1.1.6"
  }
}
